/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { DIVIDEND_DISTRIBUTOR_PROGRAM_ADDRESS } from '../programs';

export const DEPOSIT_DIVIDEND_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  203, 10, 38, 210, 120, 86, 146, 87,
]);

export function getDepositDividendDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(DEPOSIT_DIVIDEND_DISCRIMINATOR);
}

export type DepositDividendInstruction<
  TProgram extends string = typeof DIVIDEND_DISTRIBUTOR_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountDistributor extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountDistributor extends string
        ? WritableAccount<TAccountDistributor>
        : TAccountDistributor,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type DepositDividendInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
  totalSupply: bigint;
};

export type DepositDividendInstructionDataArgs = {
  amount: number | bigint;
  totalSupply: number | bigint;
};

export function getDepositDividendInstructionDataEncoder(): FixedSizeEncoder<DepositDividendInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['amount', getU64Encoder()],
      ['totalSupply', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: DEPOSIT_DIVIDEND_DISCRIMINATOR })
  );
}

export function getDepositDividendInstructionDataDecoder(): FixedSizeDecoder<DepositDividendInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['amount', getU64Decoder()],
    ['totalSupply', getU64Decoder()],
  ]);
}

export function getDepositDividendInstructionDataCodec(): FixedSizeCodec<
  DepositDividendInstructionDataArgs,
  DepositDividendInstructionData
> {
  return combineCodec(
    getDepositDividendInstructionDataEncoder(),
    getDepositDividendInstructionDataDecoder()
  );
}

export type DepositDividendInput<
  TAccountAuthority extends string = string,
  TAccountDistributor extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  distributor: Address<TAccountDistributor>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: DepositDividendInstructionDataArgs['amount'];
  totalSupply: DepositDividendInstructionDataArgs['totalSupply'];
};

export function getDepositDividendInstruction<
  TAccountAuthority extends string,
  TAccountDistributor extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof DIVIDEND_DISTRIBUTOR_PROGRAM_ADDRESS,
>(
  input: DepositDividendInput<TAccountAuthority, TAccountDistributor, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress }
): DepositDividendInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountDistributor,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? DIVIDEND_DISTRIBUTOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    distributor: { value: input.distributor ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('distributor', accounts.distributor),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getDepositDividendInstructionDataEncoder().encode(
      args as DepositDividendInstructionDataArgs
    ),
    programAddress,
  } as DepositDividendInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountDistributor,
    TAccountSystemProgram
  >);
}

export type ParsedDepositDividendInstruction<
  TProgram extends string = typeof DIVIDEND_DISTRIBUTOR_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    distributor: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: DepositDividendInstructionData;
};

export function parseDepositDividendInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedDepositDividendInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 3,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      distributor: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getDepositDividendInstructionDataDecoder().decode(instruction.data),
  };
}
