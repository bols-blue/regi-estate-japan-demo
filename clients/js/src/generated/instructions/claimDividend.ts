/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { DIVIDEND_DISTRIBUTOR_PROGRAM_ADDRESS } from '../programs';

export const CLAIM_DIVIDEND_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  15, 29, 207, 120, 153, 178, 164, 91,
]);

export function getClaimDividendDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CLAIM_DIVIDEND_DISCRIMINATOR);
}

export type ClaimDividendInstruction<
  TProgram extends string = typeof DIVIDEND_DISTRIBUTOR_PROGRAM_ADDRESS,
  TAccountInvestor extends string | AccountMeta<string> = string,
  TAccountDistributor extends string | AccountMeta<string> = string,
  TAccountInvestorTokenAccount extends string | AccountMeta<string> = string,
  TAccountClaimRecord extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountInvestor extends string
        ? WritableSignerAccount<TAccountInvestor> & AccountSignerMeta<TAccountInvestor>
        : TAccountInvestor,
      TAccountDistributor extends string
        ? WritableAccount<TAccountDistributor>
        : TAccountDistributor,
      TAccountInvestorTokenAccount extends string
        ? ReadonlyAccount<TAccountInvestorTokenAccount>
        : TAccountInvestorTokenAccount,
      TAccountClaimRecord extends string
        ? WritableAccount<TAccountClaimRecord>
        : TAccountClaimRecord,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ClaimDividendInstructionData = { discriminator: ReadonlyUint8Array };

export type ClaimDividendInstructionDataArgs = {};

export function getClaimDividendInstructionDataEncoder(): FixedSizeEncoder<ClaimDividendInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLAIM_DIVIDEND_DISCRIMINATOR })
  );
}

export function getClaimDividendInstructionDataDecoder(): FixedSizeDecoder<ClaimDividendInstructionData> {
  return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getClaimDividendInstructionDataCodec(): FixedSizeCodec<
  ClaimDividendInstructionDataArgs,
  ClaimDividendInstructionData
> {
  return combineCodec(
    getClaimDividendInstructionDataEncoder(),
    getClaimDividendInstructionDataDecoder()
  );
}

export type ClaimDividendInput<
  TAccountInvestor extends string = string,
  TAccountDistributor extends string = string,
  TAccountInvestorTokenAccount extends string = string,
  TAccountClaimRecord extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  investor: TransactionSigner<TAccountInvestor>;
  distributor: Address<TAccountDistributor>;
  /** Token-2022 トークンアカウント（KYC 状態・残高の確認に使用） */
  investorTokenAccount: Address<TAccountInvestorTokenAccount>;
  /** (investor, epoch) ごとの二重請求防止レコード */
  claimRecord: Address<TAccountClaimRecord>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getClaimDividendInstruction<
  TAccountInvestor extends string,
  TAccountDistributor extends string,
  TAccountInvestorTokenAccount extends string,
  TAccountClaimRecord extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof DIVIDEND_DISTRIBUTOR_PROGRAM_ADDRESS,
>(
  input: ClaimDividendInput<
    TAccountInvestor,
    TAccountDistributor,
    TAccountInvestorTokenAccount,
    TAccountClaimRecord,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): ClaimDividendInstruction<
  TProgramAddress,
  TAccountInvestor,
  TAccountDistributor,
  TAccountInvestorTokenAccount,
  TAccountClaimRecord,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? DIVIDEND_DISTRIBUTOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    investor: { value: input.investor ?? null, isWritable: true },
    distributor: { value: input.distributor ?? null, isWritable: true },
    investorTokenAccount: { value: input.investorTokenAccount ?? null, isWritable: false },
    claimRecord: { value: input.claimRecord ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('investor', accounts.investor),
      getAccountMeta('distributor', accounts.distributor),
      getAccountMeta('investorTokenAccount', accounts.investorTokenAccount),
      getAccountMeta('claimRecord', accounts.claimRecord),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getClaimDividendInstructionDataEncoder().encode({}),
    programAddress,
  } as ClaimDividendInstruction<
    TProgramAddress,
    TAccountInvestor,
    TAccountDistributor,
    TAccountInvestorTokenAccount,
    TAccountClaimRecord,
    TAccountSystemProgram
  >);
}

export type ParsedClaimDividendInstruction<
  TProgram extends string = typeof DIVIDEND_DISTRIBUTOR_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    investor: TAccountMetas[0];
    distributor: TAccountMetas[1];
    /** Token-2022 トークンアカウント（KYC 状態・残高の確認に使用） */
    investorTokenAccount: TAccountMetas[2];
    /** (investor, epoch) ごとの二重請求防止レコード */
    claimRecord: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: ClaimDividendInstructionData;
};

export function parseClaimDividendInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedClaimDividendInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 5,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      investor: getNextAccount(),
      distributor: getNextAccount(),
      investorTokenAccount: getNextAccount(),
      claimRecord: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getClaimDividendInstructionDataDecoder().decode(instruction.data),
  };
}
